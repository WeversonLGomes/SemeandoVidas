[wokwi]
version = 1
elf = ".pio/build/esp32dev/firmware.elf"
firmware = ".pio/build/esp32dev/firmware.bin"

# Para usar com Arduino IDE (sem PlatformIO), deixe os campos
# elf e firmware em branco e use o botão "Upload" do Wokwi IDE.
